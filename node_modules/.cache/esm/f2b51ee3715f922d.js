let Joi,User,bcrypt,CustomErrorHandler,RefreshToken,JwtService;_715‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_715‍.w("../../models/user",[["default",["User"],function(v){User=v}]]);_715‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_715‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_715‍.w("../../models/refresh",[["default",["RefreshToken"],function(v){RefreshToken=v}]]);_715‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);






class LoginController {
  async login (req, res, next) {
    const loginSchema = Joi.object({
      email: Joi.string()
        .email()
        .required(),
      password: Joi.string()
        .pattern(new RegExp('^[a-zA-Z0-9]{3,30}$'))
        .required()
    })
    const { error } = loginSchema.validate(req.body)

    if (error) {
      return next(error)
    }
    try {
      const user = await User.findOne({ email: req.body.email })
      if (!user) {
        return next(CustomErrorHandler.wrongCredentials())
      }
      const match = bcrypt.compare(req.body.password, user.password)
      if (!match) {
        return next(CustomErrorHandler.wrongCredentials())
      }
      const refreshToken = JwtService.sign(
        { _id: user._id, role: user.role },
        '1y'
      )
      await RefreshToken.create({ token: refreshToken })
      res.json(refreshToken)
    } catch (error) {
      return next(error)
    }
  }
  async logout (req, res, next) {
    const refreshSchema = Joi.object({
      refresh_token: Joi.string().required()
    })
    const { error } = refreshSchema.validate(req.body)

    if (error) {
      return next(error)
    }
    try {
      await RefreshToken.deleteOne({ token: req.body.refresh_token })
    } catch (error) {
      return next(new Error('Something went wrong in the database'))
    }
    res.json({ status: 1 })
  }
}

module.exports = new LoginController()
